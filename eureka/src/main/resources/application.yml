spring:
  application:
    name: eureka
  config:
    import: "optional:configserver:http://config-server:8071"
  # RabbitMQ 설정 추가
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
  cloud:
    bus:
      enabled: true
      refresh:
        enabled: true

# Actuator 엔드포인트 설정 추가
management:
  endpoints:
    web:
      exposure:
        include: health,info,busrefresh